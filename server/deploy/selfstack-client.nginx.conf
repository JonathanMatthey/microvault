server {
    server_name selfstack.skatkis-tech.net;

    root /var/www/selfstack.skatkis-tech.net;
    index index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    # CSP: Allow Google Sign-In and Web Monetization
    add_header Content-Security-Policy "monetization-src https://ilp.interledger-test.dev; script-src 'self' https://accounts.google.com; frame-src https://accounts.google.com; connect-src 'self' https://accounts.google.com https://content.skatkis-tech.net" always;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Disable cache for HTML
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/selfstack.skatkis-tech.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/selfstack.skatkis-tech.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = selfstack.skatkis-tech.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name selfstack.skatkis-tech.net;
    return 404; # managed by Certbot


}
